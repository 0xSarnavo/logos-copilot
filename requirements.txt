pg8000>=1.31
PyYAML>=6.0
fastmcp>=3.0
httpx>=0.27
starlette>=0.37
uvicorn>=0.29
pytest>=8.0
fastembed>=0.4           # default embedder: local BGE (semantic, CPU, $0). EMBEDDER=hash to skip it.
# voyageai>=0.3          # premium embeddings + reranker (EMBEDDER=voyage / RERANKER=voyage)
